# OAO Wiki — 에이전트 지침

이 저장소는 **LLM 이 유지하는 위키**다. 카파시 LLM Wiki 패턴을 따른다
(원문: `vault/raw/references/karpathy-llm-wiki.md`).

> **위키를 쓰는 건 너다.** 사람은 자료를 넣고, 질문하고, 방향을 정한다.
> 요약·상호참조·정리·기록은 전부 에이전트 몫이다.

---

## 1. 층과 권한 — 어기지 말 것

```
vault/
├── raw/      원본.        읽기만. 고치거나 지우지 않는다
├── wiki/     정리된 지식.  읽기·쓰기
├── schema/   운영 규칙.    읽기만. 사람이 정한다
├── index/    목차.        자동 생성물. 스크립트로만 갱신
└── log/      이력.        덧붙이기만
```

| 절대 규칙 | 이유 |
|---|---|
| **`raw/` 를 수정하지 않는다** | 원본이 바뀌면 "실제로 무엇이 오갔는가"라는 근거가 사라진다. 오탈자도 원본은 두고 정리본에 바로잡는다 |
| **`schema/` 를 수정하지 않는다** | 규칙을 지키는 쪽이 규칙을 바꾸면 규칙이 아니다. 고칠 필요가 보이면 사람에게 말한다 |
| **`personal/` 을 열지 않는다** | 회사/개인 분리 |
| **자격증명을 문서·프롬프트에 쓰지 않는다** | `.automation/*.env` 에만 두고 경로로만 참조한다 |

---

## 2. 문서를 쓸 때

**프론트매터** — `created` 는 필수, 고쳤으면 `updated` 도 갱신한다.

```yaml
---
created: YYYY-MM-DD
updated: YYYY-MM-DD
owner: 이름
purpose: 한 줄 설명        # index 의 요약으로 쓰인다
---
```

**링크** — 짧은 이름으로 건다. `[[전체-구조도]]` ⭕ / `[[../01-architecture/전체-구조도]]` ❌
파일을 옮겨도 안 깨진다. 표 안에서는 `[[문서\|별칭]]` 로 이스케이프한다.

**새 문서를 만들면 어딘가에서 반드시 링크한다.** 아무도 안 가리키는 문서는 묻힌다.

**번호 표기는 백틱으로 감싼다** — `결정 #12가` 라고 쓰면 `#12가` 라는 태그가 생긴다.
`` 결정 `#12`가 `` 로 쓴다.

**바이너리는 넣지 않는다** — PDF·이미지·PPT·녹음은 Drive 에 두고 링크만.

---

## 2-5. 일을 시작하기 전에 — 절차서부터 본다

**"이 일을 어떻게 하지?" 는 `vault/schema/할-수-있는-일.md` 에 답이 있다.**
도서관 안내판이다. 거기서 해당 플레이북으로 간다.

| 일 | 절차서 |
|---|---|
| 회의 기록 | `vault/schema/플레이북-미팅.md` |
| 질문에 답하기 | `vault/schema/query-규칙.md` |

절차서에 없는 일을 반복하게 되면 **새 플레이북으로 만들자고 사람에게 제안한다.**

---

## 3. 세 가지 오퍼레이션

| | 무엇 | 규약 |
|---|---|---|
| **ingest** | `raw/` 의 새 자료를 읽어 `wiki/` 로 정리 | `vault/schema/템플릿-가이드.md` |
| **query** | 질문에 `wiki/` 근거로 답한다. **출처 없는 문장은 쓰지 않는다** | `vault/schema/query-규칙.md` |
| **lint** | 규칙 위반을 찾아 보고. 자동 수정하지 않는다 | `vault/schema/lint-규칙.md` |

```bash
python3 .automation/wiki_lint.py     # 규칙 점검
python3 .automation/wiki_index.py    # 목차 재생성
python3 .automation/wiki_log.py <ingest|query|lint> "<대상>" --detail "..."
```

**작업 후 `log.md` 에 한 줄 남긴다.** 무엇이 언제 위키에 들어왔고 무엇이 조회됐는지가
남아야 위키가 살아 있는지 알 수 있다.

---

## 4. 판정 — 이 문서를 어디에 둘 것인가

> **이 문서는 코드가 바뀌면 같이 바뀌어야 하나?**
> **YES** → 모노레포 `docs/` (기능 명세·API·QA)
> **NO** → 이 vault (회의·결정·세일즈·파트너십)

"왜 그렇게 정했는지"는 코드가 열 번 바뀌어도 유효하다. **확정돼도 vault 에 남는다.**

---

## 5. 규칙 전문

**이 파일은 요약본이고 정본은 `vault/schema/` 다.** 판단이 서지 않으면 원문을 읽는다.

> ⚠️ **이 파일을 고쳤다면 `vault/schema/` 의 해당 문서도 같이 고칠 것.**
> 반대도 마찬가지다. 둘이 갈라지면 사람이 보는 규칙과 에이전트가 따르는 규칙이
> 달라진다 — 사람에게는 `vault/` 만 보이고 이 파일은 안 보이기 때문이다.

| | |
|---|---|
| 폴더 배치·판정 | `vault/schema/폴더-규칙.md` |
| 프론트매터 | `vault/schema/프론트매터-규격.md` |
| 링크·태그 | `vault/schema/링크-규칙.md` |
| lint 규칙 7종 | `vault/schema/lint-규칙.md` |
| 질문 답변 규약 | `vault/schema/query-규칙.md` |
| 에이전트 주기·권한 | `vault/schema/에이전트-역할.md` |
| 회의록 3종 형식 | `vault/schema/템플릿-가이드.md` |

**아직 못 하는 것**은 `vault/wiki/01-architecture/카파시-원문-대조.md` 에 정리돼 있다.
특히 ingest 가 기존 문서를 갱신하지 않는 문제는 **합의 전이라 구현하지 않는다.**
